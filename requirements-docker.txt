# Minimal requirements for Docker deployment
# Relaxed pins to avoid chromadb/crewai conflict on linux

fastapi>=0.100.0
uvicorn>=0.20.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# Anthropic + OpenAI
anthropic>=0.80.0
openai>=1.0.0

# LangChain / LangGraph / LangSmith
langchain>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.1.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langgraph>=0.1.0
langsmith>=0.1.0

# CrewAI
crewai>=1.0.0
crewai-tools>=1.0.0

# Vector DBs — relaxed so pip can resolve crewai/chromadb conflict
chromadb>=1.1.0
qdrant-client>=1.0.0

# RAG / eval
ragas>=0.1.0

# MCP
mcp>=1.0.0

# Utilities
requests>=2.28.0
httpx>=0.24.0
GitPython>=3.1.0
PyGithub>=2.0.0
beautifulsoup4>=4.12.0
tiktoken>=0.5.0
numpy>=1.24.0

# Streaming / async
sse-starlette>=1.0.0
python-multipart>=0.0.6
